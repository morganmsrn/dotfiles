#!/bin/zsh

# Misc
alias b="batcat"
alias c="cd && clear"
alias i="neofetch"
alias l="ls -aFlsh"
alias m="cmatrix -abrs"
alias n="nvim"
alias sn="sudo nvim"
alias so="source ~/.zshrc"

# Package management
alias ai="sudo apt install"
alias a="apt search"
alias check="sudo apt update && sudo apt upgrade && sudo apt autoremove && flatpak update && brew update && brew upgrade"

# Dev
alias cc="clang"
alias cpp="clang++"
alias nrd="npm run dev"
alias sail=".vendor/bin/sail"